services:
  postgres-authentication-service:
    container_name: postgres-authentication-service
    hostname: postgres-authentication-service
    image: postgres:alpine3.17
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: authentication-service
      POSTGRES_USER: ${POSTGRES_USERNAME}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /data/postgres
    volumes:
      - data-authentication-service:/data/postgres

  authentication-service:
    container_name: authentication-service
    hostname: authentication-service
    image: eann1s/authentication-service
    pull_policy: missing
    ports:
      - 8080:8080
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-authentication-service:5432/authentication-service
    depends_on:
      - postgres-authentication-service

  mongo:
    container_name: mongo
    hostname: mongo
    image: mongo:jammy
    restart: unless-stopped
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    volumes:
      - mongo:/data/db

#  profile-service: # This service needs a Dockerfile be present for building the docker image.
#    build:
#      context: .
#      dockerfile: Dockerfile
#    container_name: profile-service
#    hostname: profile-service
#    image: profile-service
#    restart: on-failure
#    ports:
#      - 8081:8081
#    environment:
#      SERVER_PORT: 8081 # The port to start the server inside the container
#      SPRING_PROFILES_ACTIVE: docker # Active profile of our app
#      SPRING_APPLICATION_NAME: profile-service
#      SPRING_DATA_MONGODB_AUTHENTICATION_DATABASE: ${MONGO_DB}
#      SPRING_DATA_MONGODB_AUTO_INDEX_CREATION: ${MONGO_AUTO_INDEX_CREATION}
#      SPRING_DATA_MONGODB_HOST: mongodb-profile-service
#      SPRING_DATA_MONGODB_PORT: 27017
#      SPRING_DATA_MONGODB_USERNAME: ${MONGO_DB_USERNAME}
#      SPRING_DATA_MONGODB_PASSWORD: ${MONGO_DB_PASSWORD}
#      SPRING_DATA_MONGODB_DATABASE: ${MONGO_DB}
#    depends_on: # This service depends on mongo service to be started
#      - mongodb-profile-service

volumes:
  data-authentication-service: {}
  mongo: {}
