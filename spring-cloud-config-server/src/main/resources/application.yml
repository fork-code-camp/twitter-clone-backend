server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fork-code-camp/microservices-config-server
          searchPaths: '{application}'

encrypt:
  keystore:
    location: classpath:key/APIKey.jks
    password: ${KEYSTORE_PASSWORD}
    alias: APIKey
